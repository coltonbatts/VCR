[project]
name = "vcr-mcp-server"
version = "0.1.0"
description = "MCP Server exposing VCR motion graphics engine to AI agents"
requires-python = ">=3.11"
dependencies = ["mcp[cli]>=1.0.0", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
